<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Job Details | Admin Panel</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.0/css/all.min.css" rel="stylesheet">
  <style>
    body {
      background-color: #f5f7fa;
      font-family: 'Poppins', sans-serif;
    }
    .content {
      margin-left: 260px;
      padding: 40px;
    }
    .job-card {
      border: none;
      border-radius: 18px;
      box-shadow: 0 6px 16px rgba(0,0,0,0.08);
      background: #fff;
      overflow: hidden;
    }
    .job-header {
      background: linear-gradient(135deg, #007bff, #00c6ff);
      color: white;
      padding: 30px;
    }
    .job-header h3 {
      margin-bottom: 5px;
      font-weight: 600;
    }
    .badge {
      font-size: 0.85rem;
      padding: 6px 10px;
      border-radius: 10px;
    }
    .job-body {
      padding: 30px;
    }
    .info-grid {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(240px, 1fr));
      gap: 20px;
    }
    .info-box {
      background: #f9fafc;
      border-radius: 12px;
      padding: 15px 20px;
      border-left: 4px solid #007bff;
    }
    .info-box i {
      color: #007bff;
      margin-right: 10px;
    }
    .info-title {
      font-size: 0.9rem;
      color: #6c757d;
    }
    .info-value {
      font-size: 1.05rem;
      font-weight: 600;
      color: #212529;
    }
    .job-desc {
      background: #fff;
      border-radius: 12px;
      padding: 20px;
      border-left: 5px solid #0d6efd;
      margin-top: 30px;
    }
  </style>
</head>
<body>

  <%- include('slider.ejs') %>

  <div class="content">
    <div class="d-flex justify-content-between align-items-center mb-4">
      <h3><i class="fa fa-briefcase text-primary me-2"></i> Job Overview</h3>
      <a href="/admin/manage_jobs" class="btn btn-outline-secondary">
        <i class="fa fa-arrow-left me-2"></i> Back
      </a>
    </div>

    <div class="job-card">
      <!-- Header -->
      <div class="job-header d-flex justify-content-between align-items-center">
        <div>
          <h3><%= job[0].job_title %></h3>
          <p class="mb-0"><i class="fa fa-building me-2"></i>Company Name: <%= job[0].company_name %></p>
        </div>
        <span class="badge <%= job.status === 'Active' ? 'bg-success' : 'bg-danger' %>">
          <%= job[0].status %>
        </span>
      </div>

      <!-- Body -->
      <div class="job-body">
        <div class="info-grid">
          <div class="info-box">
            <div class="info-title"><i class="fa fa-layer-group"></i>Category</div>
            <div class="info-value"><%= job[0].job_category %></div>
          </div>

          <div class="info-box">
            <div class="info-title"><i class="fa fa-clock"></i>Job Type</div>
            <div class="info-value"><%= job[0].job_type %></div>
          </div>

          <div class="info-box">
            <div class="info-title"><i class="fa fa-user-tie"></i>Experience</div>
            <div class="info-value"><%= job[0].experience_min %> - <%= job[0].experience_max %> yrs</div>
          </div>

          <div class="info-box">
            <div class="info-title"><i class="fa fa-users"></i>Vacancies</div>
            <div class="info-value"><%= job[0].vacancies %></div>
          </div>

          <div class="info-box">
            <div class="info-title"><i class="fa fa-link"></i>Reference</div>
            <div class="info-value">
              <a href="<%= job[0].reference_link %>" target="_blank" class="text-decoration-none text-primary">
                View Link
              </a>
            </div>
          </div>

          <div class="info-box">
            <div class="info-title"><i class="fa fa-calendar-plus"></i>Created</div>
            <div class="info-value"><%= job[0].created_at %></div>
          </div>
        </div>

        <div class="job-desc mt-4">
          <h5><i class="fa fa-file-alt text-primary me-2"></i>Job Description</h5>
          <p class="text-secondary" style="line-height:1.7;"><%= job[0].job_description %></p>

          <hr>
          <h6><i class="fa fa-code text-primary me-2"></i>Required Skills</h6>
          <p class="text-dark fw-semibold"><%= job[0].skills %></p>
        </div>
      </div>
    </div>
  </div>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
