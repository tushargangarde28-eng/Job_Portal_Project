<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Edit Job | Admin Panel</title>
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.0/css/all.min.css" rel="stylesheet">
  <style>
    body {
      background-color: #f5f7fa;
      font-family: 'Poppins', sans-serif;
    }

    .content {
      margin-left: 260px;
      padding: 40px;
      transition: all 0.3s ease;
    }

    @media (max-width: 992px) {
      .content {
        margin-left: 0;
        padding: 20px;
      }
    }

    .card {
      border: none;
      border-radius: 16px;
      box-shadow: 0 6px 14px rgba(0, 0, 0, 0.08);
    }

    .card-header {
      background: linear-gradient(135deg, #007bff, #00c6ff);
      color: white;
      border-radius: 16px 16px 0 0;
      padding: 20px 30px;
      text-align: center;
    }

    label {
      font-weight: 500;
      color: #333;
    }

    .form-control, .form-select {
      border-radius: 10px;
      transition: all 0.2s ease-in-out;
    }

    .form-control:focus, .form-select:focus {
      border-color: #007bff;
      box-shadow: 0 0 0 0.15rem rgba(0, 123, 255, 0.2);
    }

    .btn-primary {
      background: linear-gradient(135deg, #007bff, #00b4d8);
      border: none;
      border-radius: 10px;
    }

    .btn-primary:hover {
      background: linear-gradient(135deg, #0056b3, #0096c7);
    }

    .back-btn {
      border-radius: 10px;
      font-size: 14px;
    }

    /* Mobile tweaks */
    @media (max-width: 768px) {
      h3 {
        font-size: 1.2rem;
      }
      .card-header h5 {
        font-size: 1rem;
      }
      .btn {
        font-size: 0.9rem;
      }
    }
  </style>
</head>
<body>

  <%- include('slider.ejs') %>

  <div class="content">
    <div class="d-flex flex-wrap justify-content-between align-items-center mb-4">
      <h3 class="mb-2 mb-md-0"><i class="fa fa-pen-to-square text-primary me-2"></i>Edit Job</h3>
      <a href="/admin/manage_jobs" class="btn btn-outline-secondary back-btn">
        <i class="fa fa-arrow-left me-2"></i> Back
      </a>
    </div>

    <div class="card">
      <div class="card-header">
        <h5 class="mb-0"><i class="fa fa-briefcase me-2"></i>Update Job Details</h5>
      </div>

      <div class="card-body p-4">
        <form action="/admin/update_job" method="POST">
          <input type="hidden" name="job_id" value="<%= job[0].job_id %>">

          <div class="row g-3">
            <div class="col-md-6">
              <label>Job Title</label>
              <input type="text" name="job_title" class="form-control" value="<%= job[0].job_title %>" required>
            </div>

            <div class="col-md-6">
              <label>Category</label>
              <input type="text" name="job_category" class="form-control" value="<%= job[0].job_category %>" required>
            </div>

            <div class="col-md-6">
              <label>Type</label>
              <select name="job_type" class="form-select" required>
                <option value="Full-Time" <%= job[0].job_type === 'Full-Time' ? 'selected' : '' %>>Full-Time</option>
                <option value="Part-Time" <%= job[0].job_type === 'Part-Time' ? 'selected' : '' %>>Part-Time</option>
                <option value="Internship" <%= job[0].job_type === 'Internship' ? 'selected' : '' %>>Internship</option>
                <option value="Contract" <%= job[0].job_type === 'Contract' ? 'selected' : '' %>>Contract</option>
              </select>
            </div>

            <div class="col-md-3 col-6">
              <label>Min Exp (yrs)</label>
              <input type="number" name="experience_min" class="form-control" value="<%= job[0].experience_min %>" required>
            </div>

            <div class="col-md-3 col-6">
              <label>Max Exp (yrs)</label>
              <input type="number" name="experience_max" class="form-control" value="<%= job[0].experience_max %>" required>
            </div>

            <div class="col-md-8">
              <label>Skills</label>
              <input type="text" name="skills" class="form-control" value="<%= job[0].skills %>" placeholder="e.g. HTML, CSS, React" required>
            </div>

            <div class="col-md-4">
              <label>Vacancies</label>
              <input type="number" name="vacancies" class="form-control" value="<%= job[0].vacancies %>" required>
            </div>

            <div class="col-md-12">
              <label>Reference Link</label>
              <input type="url" name="reference_link" class="form-control" value="<%= job[0].reference_link %>">
            </div>

            <div class="col-12">
              <label>Job Description</label>
              <textarea name="job_description" rows="4" class="form-control" required><%= job[0].job_description %></textarea>
            </div>

            <div class="col-md-4">
              <label>Status</label>
              <select name="status" class="form-select">
                <option value="Active" <%= job[0].status === 'Active' ? 'selected' : '' %>>Active</option>
                <option value="Closed" <%= job[0].status === 'Closed' ? 'selected' : '' %>>Closed</option>
              </select>
            </div>
          </div>

          <div class="text-end mt-4">
            <button type="submit" class="btn btn-primary px-4 py-2">
              <i class="fa fa-save me-2"></i> Save Changes
            </button>
          </div>
        </form>
      </div>
    </div>
  </div>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
